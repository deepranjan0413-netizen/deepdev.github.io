{
  "name": "pressmark-functions",
  "description": "Secure backend for the PRESSMARK storefront (Razorpay + Qikink + rating aggregation)",
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^4.9.0",
    "node-fetch": "^2.7.0",
    "razorpay": "^2.9.4"
  },
  "private": true
}
